# sickfits

```txt
Frontend:
  React             (view only, basic state / templates / rendering)
  Next.js           (server-side framework: static|dynamic rendering / routing / etc)
  Apollo Client     (throws GraphQL queries at Keystone)
  Styled Components (scoped css)

Backend:
  Keystone.js       (GraphQL, Next.js)
    Node
    MongoDB
```
